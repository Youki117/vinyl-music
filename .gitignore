node_modules/
dist/
src-tauri/target/
*.local
.DS_Store
Thumbs.db

# 对拍脚本产物
tests/__screenshots__/
tests/__diff__/

# 由 ffmpeg 生成的测试音频，不入库
tests/fixtures/

# 实测用的第三方素材（约 24MB），用 scripts/fetch-real-assets.mjs 重建。
# 来源与许可记在 SOURCES.md；手写的测试歌单是我们自己的，留着。
tests/real/*
!tests/real/SOURCES.md
!tests/real/测试歌单.m3u

# codebase-memory-mcp 的索引产物（本机重建即可，别入库）
.codebase-memory/
CODEBASE_GRAPH.md

# 聚合音源脚本不入库：原样分发这类脚本，风险落在仓库上。
# 使用者自己放一份进来，或用界面上的「导入音源」。见 src/source/builtin/README.md
src/source/builtin/*.js
src/source/builtin/UPSTREAM.md

# 设计参考（录屏裁切、Figma 导出包）只在本地做视觉对拍用，不随仓库分发。
# 缺了它只影响 npm run visual，构建与运行都不受影响。
design-ref/

# 以下几项在 docs/UI-REFINEMENT-BASELINE-2026-08-21.md §4 已约定不入库，
# 之前只靠人记着不要 git add -A，这里补上强制。
# target-* 覆盖用 CARGO_TARGET_DIR 指出去的隔离构建目录（如 target-final-ui）。
src-tauri/target-*/
artifacts/
release-test/
assets/
目标效果.html
